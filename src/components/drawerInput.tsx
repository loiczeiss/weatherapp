'use client'
import {
    Drawer,
    DrawerContent,
    DrawerHeader,
    DrawerBody,

} from "@heroui/react";
export function DrawerInput(props: DrawerInputProps) {
    const {} = props;

    return (<Drawer>
        <DrawerContent>
            <DrawerHeader>

            </DrawerHeader>
            <DrawerBody></DrawerBody>
        </DrawerContent>
    </Drawer>);
}